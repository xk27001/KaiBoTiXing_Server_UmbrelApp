# Changelog

本项目的重要变更记录在此文件中。

## [1.0.5-umbrel] - 2026-09-22

### Added

- 网页设置新增“代理池验证并发数”，默认 200，并持久化到数据库 `monitor_config`。
- 动态调整代理验证线程池并发数，无需重启即可生效。

## [1.0.4-umbrel] - 2026-09-22

### Changed

- 代理池验证目标改为抖音直播页面，验证码页、空页面和异常 HTML 不再进入可用池。
- 每个页面最多请求 15 次，优先使用本次未使用且历史使用次数最少的代理，最后预留一次直连。
- 新增 `RENDER_DATA`、`__INITIAL_STATE__`、转义 JSON 和文本状态兜底解析。
- 解析失败日志增加页面长度、`roomId` 和 `__pace_f` 特征，方便定位页面结构变化。

## [1.0.3-umbrel] - 2026-09-22

### Fixed

- 命中抖音验证码页时不再直接返回未知状态，而是剔除当前代理并继续轮换其他代理。
- 每个页面最多请求 15 次（代理轮换，最后预留一次直连）。
- 所有代理和直连都失败时才返回未知状态，并输出更明确的错误原因。
- 状态为未知时仍更新“最近检测时间”，但保留上一次可信的直播状态。
- 优化验证码页识别，减少正常页面中的 `verify`/`captcha` 字符串误判。

## [1.0.2-umbrel] - 2026-09-22

### Fixed

- Web 静态资源改为禁止缓存，并增加版本查询参数，避免 Umbrel 更新后浏览器继续加载旧的 `app.js` 和 `app.css`。
- `/api/health` 返回当前 Umbrel 应用版本，便于确认容器实际运行版本。

## [1.0.1-umbrel] - 2026-09-22

### Changed

- 监控状态按直播中、未开播、未知排序。
- Web 控制台所有数据表格增加行号。
- Web 控制台支持持久化配置代理池每轮采样验证条数。
- 加深选中行的背景色，便于和普通行区分。

## [1.0.0-umbrel] - 2026-09-21

### Added

- 新增 Umbrel 社区应用商店结构。
- 新增无界面 Web 服务端 `ServerLauncher`。
- 新增浏览器控制台：监控状态、主播管理、开播记录、日志和代理池。
- 新增 MySQL 8.4 Compose 部署。
- 新增 Gitee + SSH 手动部署 Compose。
- 新增 GitHub Actions MySQL/Umbrel 镜像构建流程。
- 新增 GitHub Actions CI。
