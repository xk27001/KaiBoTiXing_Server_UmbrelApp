## 修改内容

请简要说明本 Pull Request 解决的问题。

## 主要变更

- [ ] 功能实现
- [ ] Bug 修复
- [ ] 文档更新
- [ ] 构建、CI 或部署配置
- [ ] 数据库结构变更

## 验证方式

请列出执行过的命令和结果：

```bash
mvn -B -ntp test
node --check src/main/resources/web/app.js
```

## Umbrel 检查

- [ ] `umbrel-app.yml` 版本号已同步更新
- [ ] `docker-compose.yml` 镜像标签已同步更新
- [ ] `.github/workflows/umbrel-image.yml` 镜像标签已同步更新
- [ ] 未提交数据库密码、Token 或 `.env`

## 关联 Issue

Closes #
